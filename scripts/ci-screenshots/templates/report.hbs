# 📸 Visual Regression Report

> Generated: {{generatedAt}}
> Head: `{{headBranch}}` vs Base: `{{baseBranch}}`
> Artifacts: [Download screenshots]({{artifactUrl}})

## Summary

| Metric | Count |
|--------|-------|
| 🔴 Major changes (>2%) | {{summary.major}} |
| 🟡 Minor changes (0.1–2%) | {{summary.minor}} |
| ✅ Unchanged (<0.1%) | {{summary.unchanged}} |
| 🆕 New routes | {{summary.new}} |
| ⚠️ Missing baseline | {{summary.missingBaseline}} |
| **Total routes** | **{{summary.total}}** |

---
{{#if majorChanges}}
## 🔴 Major Changes

{{#each majorChanges}}
<details>
<summary><strong>{{name}}</strong> — <code>{{route}}</code> @ {{viewport}} ({{diffPct}}% diff)</summary>

| Before | After | Diff |
|--------|-------|------|
| ![baseline]({{baselinePath}}) | ![current]({{currentPath}}) | ![diff]({{diffPath}}) |

> Pixels changed: {{pixelsDifferent}} / {{totalPixels}} ({{diffPct}}%)

</details>
{{/each}}

{{/if}}
{{#if minorChanges}}
## 🟡 Minor Changes

{{#each minorChanges}}
<details>
<summary><strong>{{name}}</strong> — <code>{{route}}</code> @ {{viewport}} ({{diffPct}}% diff)</summary>

| Before | After | Diff |
|--------|-------|------|
| ![baseline]({{baselinePath}}) | ![current]({{currentPath}}) | ![diff]({{diffPath}}) |

> Pixels changed: {{pixelsDifferent}} / {{totalPixels}} ({{diffPct}}%)

</details>
{{/each}}

{{/if}}
{{#if newRoutes}}
## 🆕 New Routes (no baseline)

{{#each newRoutes}}
<details>
<summary><strong>{{name}}</strong> — <code>{{route}}</code> @ {{viewport}}</summary>

![current]({{currentPath}})

</details>
{{/each}}

{{/if}}
{{#if unchangedRoutes}}
## ✅ Unchanged Routes

<details>
<summary>{{unchangedCount}} route/viewport combinations unchanged</summary>

{{#each unchangedRoutes}}
- `{{route}}` @ {{viewport}}
{{/each}}

</details>
{{/if}}

---
*Generated by CI Screenshot Pipeline*
